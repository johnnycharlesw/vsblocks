# Markdown Math

**Notice:** This extension is bundled with VSBlocks. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to VSBlocks's built-in markdown preview and markdown cells in notebooks.
